#!/usr/bin/perl

# Gaucho.pl

use strict;

my @version = ('0.01-dev','Gaucho Version');


&main();

sub main() {
	&Constants;
	&Common;
	&Exceptions;
	&Hooks;
	&Config;
	&URI;
	&Router;
	&Output;
	&Controller;
	&Post;
}

sub Constants { } 
sub Common { } 
sub Exceptions {  }
sub Hooks { } 
sub Config { } 
sub URI { }
sub Router { } 
sub Output { } 
sub Controller { } 
sub Post { } 
